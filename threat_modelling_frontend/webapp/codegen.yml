schema: ./schema.graphql
documents: ./src/graphql/**/*.graphql
generates:
  ./src/API.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
    config:
      withHooks: true

